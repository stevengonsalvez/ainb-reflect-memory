# archive/

Completed one-shot migration scripts, kept for reference. These ran once during their respective rollouts and are not part of the live runtime: `migrate_v2.py` (legacy v2 YAML state import), `regate_backlog.py` (re-gate the existing pending-reflections queue), `backfill_costs.py` (seed the cost timeline from historical session logs).
